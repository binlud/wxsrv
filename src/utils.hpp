#ifndef _UTILS_H_
#define _UTILS_H_

#include <iostream>
#include <string>
#include <map>
#include "log.hpp"
using namespace std;

bool jxGetData(const string &src, map<string, string> &res );

#endif
